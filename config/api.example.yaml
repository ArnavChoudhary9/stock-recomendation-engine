api:
  host: "127.0.0.1"             # bind to 0.0.0.0 for LAN access from a second machine
  port: 8000
  cors:
    allowed_origins: ["*"]      # personal use, no restriction needed
  request_id_header: "X-Request-ID"

# Kite credentials live in config/portfolio.yaml — not here.
